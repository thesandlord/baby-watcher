{
  "name": "baby-watcher",
  "private": true,
  "workspaces": [
    "shared",
    "web"
  ],
  "scripts": {
    "test": "npm run test --workspace=shared",
    "build": "npm run build --workspace=shared && npm run build --workspace=web",
    "dev:web": "npm run dev --workspace=web",
    "dev:emulators": "firebase emulators:start --only firestore,auth,hosting"
  },
  "engines": {
    "node": ">=20"
  }
}
