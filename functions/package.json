{
  "name": "@baby-watcher/functions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "24"
  },
  "scripts": {
    "baml:generate": "baml-cli generate",
    "build": "npm run baml:generate && tsc",
    "build:watch": "tsc --watch",
    "test": "npm run build && node --input-type=module --eval \"import('./lib/index.js').then((mod) => { if (!mod.extractCalendar) throw new Error('extractCalendar export missing'); console.log('functions startup ok'); })\"",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@boundaryml/baml": "^0.226.1",
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
